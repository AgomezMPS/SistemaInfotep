# Vídeos del curso (local)

Los archivos **`.mp4`** de las 20 lecciones **no se versionan en Git** (GitHub limita el tamaño de archivos y del repositorio).

Coloca aquí los vídeos originales del curso (`1 … 20` en 720p) si trabajas en local. Los subtítulos están en la raíz del repositorio: `subtitles1.srt` … `subtitles20.srt`.
